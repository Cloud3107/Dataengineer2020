Smallest notes:

Description:

There are notes of the six denominations namely , Rs 1, Rs 2, Rs 5 , Rs 10 , Rs 50, Rs 100.
if the sum of Rs N is input , write a program to compute smallest number of notes that will combine to give Rs N.

Input Format :

> The First line contains an Integer T, totalnumber of testcases.
  then follow T lines , each line contains and integer N.
  
Constraints

> 1 <= T <= 1000
> 1 <= N <= 1000000

Output Format :

> Display the smallest number of notes that will combine to give N.

Sample Input

3
1200
500
242

Sample Output 

12
5
7

Execution time limit 



Solution : Langugae - c++ 14

#include <cstdio>
#include <iostream>
#include <algorithm>
#include <cmath>
#include <vector>

using namespace std;
int fun(int coins[],int m, int V)
{
   int table[V+1];
   table[0]= 0;
   for(int i=1;i<=V;i++)
   {
     for (int j =o;j<m;j++)
       if(coins[j]<=i)
       {
       int sub_res = table[i-coins[j]];
       if(sub_res!=INT_MAX && sub_res +1 < table[i])
       table[i]=sub_res+1;
       }
   }
   return table[V];
   
 }
     

int main(){
  int t;
  int coins[] = {100,50,10,5,2,1};
  int m =6;

  cin >> t;
  while(t--)
  {
    int n;
    cin>>n;
    cout<<fun(coins,m,n)<<end1;
    
   }
   return 0;
   
}
